{
  "name": "typediagram",
  "version": "0.9.0",
  "description": "typeDiagram CLI — parse a .td source file and emit SVG",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nimblesite/typeDiagram.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "bin": {
    "typediagram": "./dist/bin.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.build.json"
  },
  "dependencies": {
    "typediagram-core": "0.9.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "@types/node": "^25.9.3"
  }
}
