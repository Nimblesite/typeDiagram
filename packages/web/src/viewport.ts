// [WEB-VIEWPORT] The web host consumes the framework's shared canvas behavior.
export { createViewport, setViewportContent, type ViewportControls, type ViewportState } from "typediagram-core/editor";
